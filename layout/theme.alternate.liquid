<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% comment %} <link rel="stylesheet" href="{{ 'style.css' | asset_url }}"> {% endcomment %}
    {{ 'style.css' | asset_url | stylesheet_tag }}
    {{ content_for_header }}
  </head>

  <body class="page">
    {% section 'alternate-header' %}

    {% render 'alternate-breadcrumbs' %}

    <main class="page__main main container" id="main">
      {{ content_for_layout }}
    </main>

    {% section 'alternate-footer' %}

    {{ 'index.js' | asset_url | script_tag }}
  </body>
</html>
